apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: search-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: search
  endpoints:
    - port: http
      path: /metrics
      interval: 15s